# colab_keeper package

This package contains helpers to automate opening a Colab notebook, triggering `Run all`, and keeping the session alive.

Configuration is done via environment variables or by editing `colab_keeper/config.py`.

See `examples/env.example` for recommended variables.
